apiVersion: v1
kind: Service
metadata:
  name: ai-workflow-api-service
  namespace: ai-workflow
spec:
  type: LoadBalancer
  selector:
    app: ai-workflow-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
